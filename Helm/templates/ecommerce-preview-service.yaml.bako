apiVersion: v1
kind: Service
metadata:
  name: ecommerce-preview
  namespace: default
spec:
  selector:
    app: Ecommerce
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP  # Use ClusterIP for the preview, no need to expose it externally
